import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const TabTwoRewards = () => {
  return (
    <View style={styles.container}>
      <Text>TabTwoRewards</Text>
    </View>
  );
};

export default TabTwoRewards;

const styles = StyleSheet.create({
  container: {
    backgroundColor: 'aqua',
  },
});
