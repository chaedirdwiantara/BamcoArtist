import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const TabOneReward = () => {
  return (
    <View style={styles.container}>
      <Text>TabOneReward</Text>
    </View>
  );
};

export default TabOneReward;

const styles = StyleSheet.create({
  container: {
    backgroundColor: 'brown',
  },
});
